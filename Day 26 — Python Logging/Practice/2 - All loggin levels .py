import logging

logging.basicConfig(level=logging.DEBUG)

logging.debug("Debug a program .")
logging.info("Some inportent info .")
logging.warning("Give warning for some operation .")
logging.error("Error occur in some cases .")
logging.critical("Critical problem program can't be executed .")